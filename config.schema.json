{
    "pluginAlias": "SmartLedStrip",
    "pluginType": "accessory",
    "headerDisplay": "[RPi](https://www.raspberrypi.org) GPIO based LED Strip plugin for [Homebridge](https://github.com/nfarina/homebridge).",
    "footerDisplay": "Raise [Issues](https://github.com/Sunoo/homebridge-gpio-rgbw-ledstrip/issues) or submit [Pull Requests](https://github.com/Sunoo/homebridge-gpio-rgbw-ledstrip/pulls) on [Project Page](https://github.com/Sunoo/homebridge-gpio-rgbw-ledstrip).",
    "schema": {
        "type": "object",
        "properties": {
            "name": {
                "title": "Name",
                "type": "string",
                "required": true,
                "default": "LED Strip",
                "description": "A unique name for the accessory. It will be used as the accessory name in HomeKit."
            },
            "rPin": {
                "title": "Red Pin",
                "type": "integer",
                "required": true,
                "placeholder": 26,
                "description": "GPIO pin that is used to set red value."
            },
            "gPin": {
                "title": "Green Pin",
                "type": "integer",
                "required": true,
                "placeholder": 19,
                "description": "GPIO pin that is used to set green value."
            },
            "bPin": {
                "title": "Blue Pin",
                "type": "integer",
                "required": true,
                "placeholder": 16,
                "description": "GPIO pin that is used to set blue value."
            },
            "wPin": {
                "title": "White Pin",
                "type": "integer",
                "required": true,
                "placeholder": 20,
                "description": "GPIO pin that is used to set white value."
            }
        }
    }
}
